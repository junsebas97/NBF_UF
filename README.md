# Nonlinear output-only filter

[This](main.m) is the implementation of the nonlinear output-only filter to a SDOF system and 3-DOFs chain of masses. The filter uses the UT for the filtering and self-starting time-integration algorithms to construct the state-space models.